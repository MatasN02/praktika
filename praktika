class AcademicSystem:
    # ... (existing code)

    def validate_input(self, *args):
        for arg in args:
            if arg is None or (isinstance(arg, str) and not arg.strip()):
                raise ValueError("Invalid input. Please provide valid values.")

    def register_student(self, first_name, last_name, group_id):
        self.validate_input(first_name, last_name, group_id)
        # ... (existing code)

    # ... (similar changes for other methods)
